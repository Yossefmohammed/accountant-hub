# PostgreSQL (default — use your PostgreSQL connection string)
DATABASE_URL="postgresql://user:password@localhost:5432/accountant_hub"

# SQLite (optional — uncomment and use instead of PostgreSQL)
# DATABASE_URL="file:./dev.db"

# MySQL (optional)
# DATABASE_URL="mysql://root:password@localhost:3306/accountant_hub"

JWT_SECRET="change-this-to-a-long-random-string-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
